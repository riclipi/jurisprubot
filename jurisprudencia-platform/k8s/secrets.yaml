apiVersion: v1
kind: Secret
metadata:
  name: jurisprudencia-secrets
  namespace: jurisprudencia
type: Opaque
stringData:
  # Database Password
  DB_PASSWORD: "changeme-production-password"
  
  # JWT Secret
  JWT_SECRET_KEY: "changeme-production-jwt-secret-key"
  
  # Redis Password (if used)
  REDIS_PASSWORD: ""